TODO LIST

- sortie
j'ai terminé a priori de gérer la méthode dans le repo
afficher la quantité réservée dans l'état de stock

il faut modifier le JS pour créer et envoyer le tableau des pré-réservations actuelles sur le code
de la ligne. On enverra alors ce tableau et on récupèrera les valeurs disponibles.

A la validation du formulaire, on créera createOutgoing.

A la destruction d'une outgoing, on effacera les rows correspondantes (même ID + type="outgoing")

A la confirmation (status = "shipped"), on déduira des différents stocks les valeurs 

peaufiner le responsive
